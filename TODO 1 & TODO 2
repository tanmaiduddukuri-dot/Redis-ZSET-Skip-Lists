SkipNode search(int score) {
    SkipNode cur = head;

    for (int i = currentLevel; i >= 0; i--) {
        while (cur.forward[i] != null &&
               cur.forward[i].score < score) {
            cur = cur.forward[i];
        }
    }

    cur = cur.forward[0];

    if (cur != null && cur.score == score)
        return cur;

    return null;
}
